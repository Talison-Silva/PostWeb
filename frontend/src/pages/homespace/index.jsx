import { styled } from "styled-components";

const HomeSpace = styled.section`
  color: white;
`;

export default ({ id, close }) => {
  return (
    <HomeSpace>
      <p>hello</p>
    </HomeSpace>
  );
};
